package org.valkyrienskies.core.util

interface DoubleTernaryConsumer {
    fun accept(x: Double, y: Double, z: Double)
}

class Spec // TODO remove, checkstyle crying about file name not equal to interface name
